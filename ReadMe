In summary, this contains different concepts to write a sorting program.

bubbleSort01.ts implements the sorting logic directly.
bubbleSort02.ts refactors the sorting logic into a function showcasing the general concept for a reusable block of code. 
bubbleSort03.ts introduces generics, providing flexibility to the sorting logic by allowing it to work with different types. 
bubbleSort04.ts incorporates lambda functions, offering a concise and anonymous way to express sorting logic, especially useful for short and simple operations.
